# Git-native CMS sandbox content

Public demonstration content consumed by the Next.js and Astro playgrounds in
[`DMTcorp/git-native-cms`](https://github.com/DMTcorp/git-native-cms).

Content uses deterministic YAML documents with locale overlays. Editorial mutations happen on
Change branches; reviewed changes are squashed into `staging`, then released to `main`.
